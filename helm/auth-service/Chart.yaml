apiVersion: v2
name: auth-service
description: Ktor-based auth service with GraphQL API
type: application
version: 0.1.0
appVersion: "1.0.0"

# Dragonfly (Redis-compatible KV store) деплоится отдельным helm-релизом.
# Инструкция: helm/dragonfly/README.md
